import 'package:flutter/material.dart';

/// 프로젝트에서 공통으로 쓸 간단 색상 모음
class AppColors {
  AppColors._(); // 인스턴스 생성 방지

  static const Color mainColor = Color(0xFFFF8040); // 브랜드/포인트
  static const Color background = Colors.white;   // 기본 배경
}
